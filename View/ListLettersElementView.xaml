<UserControl x:Class="WpfMailSender.View.ListLettersElementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfMailSender.View"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="200">
    <DockPanel>
        <Button x:Name="btnEdit" DockPanel.Dock="Left">
            <Image Source="../Images/edit.png" Margin="2"/>
        </Button>
        <TextBlock Text="{Binding SendTime}"/>
        <Button x:Name="btnDelete" Width="20" HorizontalAlignment="Right">
            <Image Source="../Images/delete.png" Margin="2"/>
        </Button>
    </DockPanel>
</UserControl>
